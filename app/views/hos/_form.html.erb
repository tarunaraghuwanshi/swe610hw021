<%= form_for(@ho) do |f| %>
  <% if @ho.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ho.errors.count, "error") %> prohibited this ho from being saved:</h2>

      <ul>
      <% @ho.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
